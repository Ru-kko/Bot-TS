{
  "name": "@bot/server",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc -p ./tsconfig.build.json",
    "build-w": "tsc -w -p ./tsconfig.build.json",
    "start": "node ./dist/index.js"
  },
  "dependencies": {
    "@bot/api": "1.0.0",
    "@bot/bot": "1.0.0",
    "@bot/config": "1.0.0"
  },
  "devDependencies": {
    "typescript": "^4.6.4"
  }
}
